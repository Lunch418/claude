# Запрет прямого HTTP-доступа к модулям приложения.
# Файлы отдаёт только PHP (index.php?m=Asset&a=script) после авторизации.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
