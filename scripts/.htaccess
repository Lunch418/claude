# Прямой HTTP-доступ запрещён — файлы используются только PHP-бэкендом.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
